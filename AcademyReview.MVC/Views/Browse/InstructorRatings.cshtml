@model IEnumerable<AcademyReview.Models.RatingModels.Instructor.InstructorRatingListItem>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Score)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
        @*<th>
            @Html.DisplayNameFor(model => model.IsUserOwned)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.UserId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.InstructorId)
        </th>*@
        <th>
            @Html.DisplayNameFor(model => model.FullName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CreatedBy)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
            @Html.HiddenFor(modelItem => item.RatingId)
            @Html.HiddenFor(modelItem => item.IsUserOwned)
            @Html.HiddenFor(modelItem => item.UserId)
            @Html.HiddenFor(modelItem => item.InstructorId)
        <td>
            @Html.DisplayFor(modelItem => item.Score)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FullName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CreatedBy)
        </td>
        <td>
           | @Html.ActionLink("Edit", "EditInstructorRating", new {  id=item.RatingId  }) |
           | @Html.ActionLink("Delete", "DeleteInstructorRating", new {  id=item.RatingId  }) |
        </td>
    </tr>
}

</table>
